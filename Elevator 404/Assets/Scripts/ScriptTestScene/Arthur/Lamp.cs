using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class Lamp : ElectricObjects
{

    public bool lampOn2;
    public Material onMat;
    public Material offMat;

    public enum LampState
    {
        On,
        Off,
        Broken,
        TransformingIntoSpaceShip
    }

    public int myState = 1346273;


    public LampState state;
    // Start is called before the first frame update
    void Start()
    {
        switch (state)
        {
            case LampState.On:
                // do this
                break;
            case LampState.Off:
                // do this
                break;
            case LampState.Broken:
                // ndo this
                break;
        }



        switch (myState)
        {
            case 1:
                // do this
                break;
            case 2:
                // do this
                break;
            case 3:
                // ndo this
                break;
        }
        if (state == LampState.On)
        {
            //
            state = LampState.TransformingIntoSpaceShip;
        }
        else if(state == LampState.Off)
        {
            //
        }
    }

    public void SetLampMat(bool on)
    {
        if (on)
        {
            GetComponent<Renderer>().material = onMat;
        }
        else
        {
            GetComponent<Renderer>().material = offMat;
        }
    }

}
*/
